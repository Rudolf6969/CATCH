---
phase: 01-foundation-auth
plan: 06
type: execute
wave: 4
depends_on:
  - 01-04
  - 01-05
files_modified:
  - app/_layout.tsx
  - src/lib/posthog.ts
autonomous: false
requirements:
  - INFRA-07

user_setup:
  - service: posthog
    why: "Analytics event tracking pre core akcie (app_opened, user_registered, user_logged_in)"
    env_vars:
      - name: EXPO_PUBLIC_POSTHOG_API_KEY
        source: "PostHog Dashboard → Project Settings → API Keys → Project API Key (phc_...)"
      - name: EXPO_PUBLIC_POSTHOG_HOST
        source: "Pre SK/CZ EU GDPR compliance: https://eu.posthog.com (NIE https://us.posthog.com)"
    dashboard_config:
      - task: "Vytvoriť PostHog projekt"
        location: "https://eu.posthog.com → New Project → nazov 'CATCH'"
  - service: firebase
    why: "Firebase Auth + Firestore + Storage — google-services.json a GoogleService-Info.plist sú placeholder files"
    env_vars: []
    dashboard_config:
      - task: "Vytvoriť Firebase projekt"
        location: "https://console.firebase.google.com → Add Project → 'catch-app-sk'"
      - task: "Pridať Android app"
        location: "Firebase Console → Project Settings → Add App → Android → package: sk.catchapp.app"
      - task: "Pridať iOS app"
        location: "Firebase Console → Project Settings → Add App → iOS → bundleId: sk.catchapp.app"
      - task: "Stiahnuť google-services.json"
        location: "Firebase Console → Android app → Download google-services.json → umiestniť do root projektu"
      - task: "Stiahnuť GoogleService-Info.plist"
        location: "Firebase Console → iOS app → Download GoogleService-Info.plist → umiestniť do root projektu"
      - task: "Zapnúť Email/Password authentication"
        location: "Firebase Console → Authentication → Sign-in method → Email/Password → Enable"
      - task: "Nasadiť Firestore security rules"
        location: "Firebase Console → Firestore Database → Rules → Paste obsah z firestore.rules → Publish"
      - task: "Vytvoriť Firestore database"
        location: "Firebase Console → Firestore Database → Create Database → Production mode → europe-west1 (Frankfurt, blizko SK)"

must_haves:
  truths:
    - "PostHog je inicializovaný v root layout s EU host pre GDPR"
    - "app_opened event sa zavolá pri každom spustení appky"
    - "user_registered event sa zavolá po úspešnej registrácii"
    - "user_logged_in event sa zavolá po úspešnom prihlásení"
    - "EAS dev build checkpoint overí celý auth flow na reálnom zariadení"
  artifacts:
    - path: "src/lib/posthog.ts"
      provides: "PostHog client singleton s capture helper funkciou"
      exports: ["posthog", "captureEvent"]
    - path: "app/_layout.tsx"
      provides: "PostHogProvider wrapper + app_opened event pri inicializácii"
  key_links:
    - from: "app/_layout.tsx"
      to: "src/lib/posthog.ts"
      via: "import { posthog } from '@/lib/posthog'"
      pattern: "PostHogProvider"
    - from: "app/(auth)/register.tsx"
      to: "src/lib/posthog.ts"
      via: "captureEvent('user_registered', { method: 'email' })"
      pattern: "user_registered"
---

<objective>
Pridať PostHog analytics do root layoutu a kľúčových auth eventoch. Potom overiť celý Phase 1 flow na reálnom zariadení cez EAS dev build.

Purpose: Analytics sú potrebné od dňa 1 — retroaktívne pridávanie eventov je 3x drahšie. EAS dev build checkpoint overí že Firebase native moduly fungujú na skutočnom zariadení.
Output: PostHog inicializovaný s 3 core eventami. EAS dev build overený na Android zariadení.
</objective>

<execution_context>
@C:/Users/Počítač/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Počítač/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Počítač/Documents/catch-app/.planning/phases/01-foundation-auth/01-CONTEXT.md
@C:/Users/Počítač/Documents/catch-app/.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<interfaces>
<!-- Existujúci app/_layout.tsx z Plan 04 -->
```typescript
// app/_layout.tsx (aktuálny stav po Plan 04)
import { Stack } from 'expo-router';
import { QueryClientProvider } from '@tanstack/react-query';
import { useAuthStore } from '@/stores/auth.store';
// ... Stack.Protected guard={!!user}
```

<!-- posthog-react-native API -->
```typescript
import PostHog, { PostHogProvider } from 'posthog-react-native';
// Init:
const posthog = new PostHog('phc_API_KEY', { host: 'https://eu.posthog.com' });
// Capture:
posthog.capture('event_name', { prop: 'value' });
// Provider:
<PostHogProvider client={posthog}>{children}</PostHogProvider>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: PostHog analytics + app/_layout.tsx aktualizácia</name>
  <files>src/lib/posthog.ts, app/_layout.tsx, app/(auth)/register.tsx, app/(auth)/login.tsx</files>
  <action>
**src/lib/posthog.ts** — PostHog singleton:
```typescript
import PostHog from 'posthog-react-native';

// API key z .env (EXPO_PUBLIC_ prefix = dostupné na klientovi)
const POSTHOG_KEY = process.env.EXPO_PUBLIC_POSTHOG_API_KEY ?? '';
const POSTHOG_HOST = process.env.EXPO_PUBLIC_POSTHOG_HOST ?? 'https://eu.posthog.com';

// Singleton — inicializovaný raz pri importe
export const posthog = new PostHog(POSTHOG_KEY, {
  host: POSTHOG_HOST,
  // Batch eventy pre efektivitu
  flushAt: 20,
  flushInterval: 10000,
  // Disabled v development ak nie je API key
  disabled: !POSTHOG_KEY,
});

// Type-safe event helper
type CoreEvent =
  | 'app_opened'
  | 'user_registered'
  | 'user_logged_in'
  | 'user_logged_out'
  | 'catch_created'
  | 'post_published'
  | 'listing_created'
  | 'premium_purchased';

export function captureEvent(event: CoreEvent, properties?: Record<string, unknown>): void {
  posthog.capture(event, properties);
}
```

**app/_layout.tsx** — pridať PostHogProvider + app_opened event:

Aktualizovať existujúci app/_layout.tsx — pridať PostHog wrapper okolo celého stromu. Importovať `PostHogProvider` z `posthog-react-native` a `posthog` singleton z `@/lib/posthog`:

```typescript
import { Stack } from 'expo-router';
import { useEffect } from 'react';
import auth from '@react-native-firebase/auth';
import * as SplashScreen from 'expo-splash-screen';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { PostHogProvider } from 'posthog-react-native';
import { useAuthStore } from '@/stores/auth.store';
import { posthog, captureEvent } from '@/lib/posthog';

SplashScreen.preventAutoHideAsync();

const queryClient = new QueryClient({
  defaultOptions: {
    queries: { retry: 2, staleTime: 1000 * 60 * 5 },
  },
});

export default function RootLayout() {
  const { user, setUser, setInitialized, initialized } = useAuthStore();

  useEffect(() => {
    const unsubscribe = auth().onAuthStateChanged((firebaseUser) => {
      setUser(firebaseUser);
      setInitialized(true);
    });
    return unsubscribe;
  }, [setUser, setInitialized]);

  useEffect(() => {
    if (initialized) {
      SplashScreen.hideAsync();
      // Track app_opened pri každom spustení (po Firebase init)
      captureEvent('app_opened', { authenticated: !!user });
    }
  }, [initialized, user]);

  if (!initialized) return null;

  return (
    <PostHogProvider client={posthog}>
      <QueryClientProvider client={queryClient}>
        <Stack screenOptions={{ headerShown: false }}>
          <Stack.Protected guard={!!user}>
            <Stack.Screen name="(tabs)" />
          </Stack.Protected>
          <Stack.Protected guard={!user}>
            <Stack.Screen name="(auth)" />
          </Stack.Protected>
        </Stack>
      </QueryClientProvider>
    </PostHogProvider>
  );
}
```

**Aktualizovať app/(auth)/register.tsx** — pridať user_registered event po úspešnej registrácii:

V `handleRegister` funkcie, PO úspešnom `firestore().collection('users').doc(...).set(...)`, pridať:
```typescript
import { captureEvent } from '@/lib/posthog';
// ... po Firestore set():
captureEvent('user_registered', { method: 'email' });
```

**Aktualizovať app/(auth)/login.tsx** — pridať user_logged_in event:

V `handleLogin` funkcie, PO úspešnom `auth().signInWithEmailAndPassword(...)`, pridať:
```typescript
import { captureEvent } from '@/lib/posthog';
// ... po signIn:
captureEvent('user_logged_in', { method: 'email' });
```

**Vytvoriť .env súbor** s PostHog placeholder (developer vyplní reálny kľúč):
```
EXPO_PUBLIC_POSTHOG_API_KEY=phc_REPLACE_WITH_YOUR_KEY
EXPO_PUBLIC_POSTHOG_HOST=https://eu.posthog.com
EXPO_USE_FAST_RESOLVER=1
```

Pridať `.env` do `.gitignore` ak ešte nie je:
```
echo ".env" >> .gitignore
echo ".env.local" >> .gitignore
```

Vytvoriť `.env.example` (commitnúť do repo):
```
EXPO_PUBLIC_POSTHOG_API_KEY=phc_YOUR_KEY_HERE
EXPO_PUBLIC_POSTHOG_HOST=https://eu.posthog.com
EXPO_USE_FAST_RESOLVER=1
```
  </action>
  <verify>
    <automated>cd "C:/Users/Počítač/Documents/catch-app" && node -e "const fs = require('fs'); const content = fs.readFileSync('app/_layout.tsx', 'utf8'); console.log('PostHogProvider:', content.includes('PostHogProvider')); console.log('app_opened:', content.includes('app_opened')); console.log('posthog import:', content.includes('posthog'));"</automated>
  </verify>
  <done>src/lib/posthog.ts existuje s PostHog singleton a captureEvent helper. app/_layout.tsx má PostHogProvider wrapper a app_opened event. register.tsx zavolá user_registered. login.tsx zavolá user_logged_in. .env.example je v repo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 1 je kompletný:
    - Expo SDK 53 projekt s react-native-firebase v21
    - EAS Build profily (development, preview, production)
    - Design system (theme.ts, i18n SK/CZ, UI primitives)
    - Auth flow (login, register, forgot-password so Stack.Protected)
    - 5-tabová navigácia s custom FAB center button
    - PostHog analytics (app_opened, user_registered, user_logged_in)

    PRED overením: Developer musí nastaviť Firebase projekt a PostHog (pozri user_setup sekciu).
  </what-built>
  <how-to-verify>
    **Krok 1 — Firebase setup (ak ešte nespravené):**
    1. Vytvor Firebase projekt na console.firebase.google.com
    2. Pridaj Android app (package: sk.catchapp.app), stiahni google-services.json do root
    3. Zapni Email/Password authentication v Firebase Console
    4. Vytvor Firestore database (europe-west1 región)

    **Krok 2 — EAS dev build pre Android:**
    ```bash
    cd "C:/Users/Počítač/Documents/catch-app"
    eas build --platform android --profile development
    ```
    Počkaj na dokončenie (5–15 minút). Stiahni APK a nainštaluj na Android zariadenie.

    **Krok 3 — Spustiť Metro bundler:**
    ```bash
    npx expo start --dev-client
    ```

    **Krok 4 — Overiť na zariadení:**
    1. Spustiť appku → animated splash → login screen (žiadny flash iného screen)
    2. Kliknúť "Registruj sa" → register screen s 3 políčkami
    3. Zadať testovací email + heslo 8+ znakov → kliknúť Registrovať → žiadny crash
    4. Skontrolovať Firebase Console → Authentication → Users → nový user sa objaví
    5. Skontrolovať email → verifikačný email dorazí
    6. Prihlásiť sa → automatický redirect na Podmienky tab
    7. Overiť 5 tabov: Podmienky / Feed / ⊕ FAB (oranžový) / Denník / Bazár — všetky navigovateľné
    8. FAB tap → Pridať screen s "Čoskoro" placeholder
    9. Podmienky tab má dummy weather widget (nie biely screen)

    **Krok 5 — Session persistencia:**
    1. Zavrieť appku
    2. Znova otvoriť → priamo Podmienky tab (žiadny login screen)

    **Krok 6 — PostHog (ak API key nastavený):**
    1. Otvoriť PostHog Dashboard → Live Events
    2. Overiť `app_opened` event po spustení appky
  </how-to-verify>
  <resume-signal>
    Napíš "approved" ak všetko funguje, alebo popíš problémy (napr. "crash pri registrácii", "tab bar sa nezobrazuje", "FAB nie je oranžový")
  </resume-signal>
</task>

</tasks>

<verification>
```bash
cd "C:/Users/Počítač/Documents/catch-app"
npx jest 2>&1 | tail -20
# Overiť .env.example existuje
ls .env.example .gitignore
```
</verification>

<success_criteria>
- src/lib/posthog.ts exportuje posthog singleton a captureEvent helper
- app/_layout.tsx má PostHogProvider wrapper + app_opened event
- app/(auth)/register.tsx zavolá captureEvent('user_registered')
- app/(auth)/login.tsx zavolá captureEvent('user_logged_in')
- .env.example je v repo, .env je v .gitignore
- EAS dev build prebehol bez chýb
- Na zariadení: splash → login → register → email verifikácia → login → 5 tabov
- Session pretrváva po reštarte
</success_criteria>

<output>
Po dokončení vytvoriť `.planning/phases/01-foundation-auth/01-06-SUMMARY.md` s:
- Výsledok checkpoint overenia (approved/issues)
- Zoznam overených behaviors na zariadení
- Firebase projekt ID a región
- PostHog projekt URL (ak nastavený)
- Akékoľvek problémy a ich riešenia
- Celkový stav Phase 1 (COMPLETE / NEEDS_FIX)
</output>
