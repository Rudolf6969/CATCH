---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - jest.config.js
  - __tests__/unit/theme.test.ts
  - __tests__/unit/i18n.test.ts
  - __tests__/unit/auth/guard.test.ts
  - __tests__/unit/auth/auth.test.ts
  - __mocks__/@react-native-firebase/auth.ts
  - __mocks__/@react-native-firebase/firestore.ts
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "Jest beží bez chýb (npx jest --passWithNoTests)"
    - "Firebase moduly sú mockované — testy nepotrubujú reálne Firebase pripojenie"
    - "Stub testy pre auth a theme sú pripravené ako RED (failing) pred implementáciou"
  artifacts:
    - path: "jest.config.js"
      provides: "jest-expo preset, moduleNameMapper pre @/ alias, transform ignore patterns"
    - path: "__mocks__/@react-native-firebase/auth.ts"
      provides: "Mock Firebase Auth (signIn, signOut, onAuthStateChanged, createUser)"
    - path: "__mocks__/@react-native-firebase/firestore.ts"
      provides: "Mock Firestore (collection, doc, set, get)"
    - path: "__tests__/unit/auth/auth.test.ts"
      provides: "Stub testy pre AUTH-01, AUTH-02, AUTH-03, AUTH-06"
    - path: "__tests__/unit/auth/guard.test.ts"
      provides: "Stub test pre AUTH-05 (Stack.Protected presmeruje neprihlásených)"
    - path: "__tests__/unit/theme.test.ts"
      provides: "Stub test pre INFRA-04 (theme.ts exportuje tokeny)"
    - path: "__tests__/unit/i18n.test.ts"
      provides: "Stub test pre INFRA-03 (SK default locale)"
  key_links:
    - from: "jest.config.js"
      to: "__mocks__"
      via: "moduleNameMapper alebo automock"
      pattern: "moduleNameMapper"
---

<objective>
Vytvoriť test infraštruktúru (Jest + Firebase mocks + stub testy) ako Wave 0 pre celú Phase 1. Testy sú RED pred implementáciou — implementácia ich postupne spraví GREEN.

Purpose: Každý ďalší plan potrebuje Nyquist-compliant automated verify príkazy. Bez jest.config.js a mockov nemôžu bežať žiadne unit testy.
Output: Funkčná jest infraštruktúra s Firebase mockmi a stub testmi pre všetky auth + infra požiadavky.
</objective>

<execution_context>
@C:/Users/Počítač/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Počítač/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Počítač/Documents/catch-app/.planning/phases/01-foundation-auth/01-VALIDATION.md
@C:/Users/Počítač/Documents/catch-app/.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Jest konfigurácia + Firebase mocks</name>
  <files>jest.config.js, package.json (devDependencies), __mocks__/@react-native-firebase/auth.ts, __mocks__/@react-native-firebase/firestore.ts, __mocks__/expo-localization.ts</files>
  <action>
**Krok 1 — Inštalovať jest-expo + testing library:**
```bash
cd "C:/Users/Počítač/Documents/catch-app"
npx expo install jest-expo
npm install --save-dev @testing-library/react-native @types/jest
```

**Krok 2 — jest.config.js:**
```javascript
const { getDefaultConfig } = require('jest-expo/jest-preset');

/** @type {import('jest').Config} */
module.exports = {
  ...getDefaultConfig(),
  testMatch: ['**/__tests__/**/*.test.{ts,tsx}'],
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/src/$1',
  },
  transformIgnorePatterns: [
    'node_modules/(?!((jest-)?react-native|@react-native(-community)?)|expo(nent)?|@expo(nent)?/.*|@expo-google-fonts/.*|react-navigation|@react-navigation/.*|@unimodules/.*|unimodules|sentry-expo|native-base|react-native-svg)',
  ],
  setupFilesAfterFramework: [],
  testEnvironment: 'node',
};
```

**Krok 3 — package.json jest script:**
Pridať do scripts v package.json:
```json
"test": "jest",
"test:watch": "jest --watchAll=false",
"test:unit": "jest --testPathPattern='__tests__/unit'"
```

**Krok 4 — Mock pre @react-native-firebase/auth:**
Vytvoriť `__mocks__/@react-native-firebase/auth.ts`:
```typescript
const mockUser = {
  uid: 'test-uid-123',
  email: 'test@catchapp.sk',
  emailVerified: false,
  sendEmailVerification: jest.fn().mockResolvedValue(undefined),
};

const mockAuth = {
  currentUser: null as typeof mockUser | null,
  signInWithEmailAndPassword: jest.fn().mockResolvedValue({ user: mockUser }),
  createUserWithEmailAndPassword: jest.fn().mockResolvedValue({ user: mockUser }),
  signOut: jest.fn().mockResolvedValue(undefined),
  sendPasswordResetEmail: jest.fn().mockResolvedValue(undefined),
  onAuthStateChanged: jest.fn((callback: (user: typeof mockUser | null) => void) => {
    // Simulovať okamžitý callback s null (neprihlásený)
    setTimeout(() => callback(null), 0);
    return jest.fn(); // unsubscribe funkcia
  }),
};

const authModule = jest.fn(() => mockAuth);
(authModule as any).default = authModule;

// Export named + default
module.exports = authModule;
module.exports.default = authModule;

export { mockUser, mockAuth };
export default authModule;
```

**Krok 5 — Mock pre @react-native-firebase/firestore:**
Vytvoriť `__mocks__/@react-native-firebase/firestore.ts`:
```typescript
const mockDocRef = {
  set: jest.fn().mockResolvedValue(undefined),
  get: jest.fn().mockResolvedValue({ exists: false, data: () => null }),
  update: jest.fn().mockResolvedValue(undefined),
  delete: jest.fn().mockResolvedValue(undefined),
};

const mockCollectionRef = {
  doc: jest.fn(() => mockDocRef),
  add: jest.fn().mockResolvedValue(mockDocRef),
  get: jest.fn().mockResolvedValue({ docs: [] }),
};

const mockFirestore = {
  collection: jest.fn(() => mockCollectionRef),
  FieldValue: {
    serverTimestamp: jest.fn(() => new Date()),
    arrayUnion: jest.fn((...items: any[]) => items),
    arrayRemove: jest.fn((...items: any[]) => items),
  },
};

const firestoreModule = jest.fn(() => mockFirestore);
(firestoreModule as any).default = firestoreModule;
(firestoreModule as any).FieldValue = mockFirestore.FieldValue;

module.exports = firestoreModule;
module.exports.default = firestoreModule;
module.exports.FieldValue = mockFirestore.FieldValue;

export { mockFirestore, mockDocRef, mockCollectionRef };
export default firestoreModule;
```

**Krok 6 — Mock pre expo-localization:**
Vytvoriť `__mocks__/expo-localization.ts`:
```typescript
export const getLocales = jest.fn(() => [
  { languageCode: 'sk', countryCode: 'SK', languageTag: 'sk-SK' },
]);
export const locale = 'sk-SK';
export const locales = [{ languageCode: 'sk' }];
```
  </action>
  <verify>
    <automated>cd "C:/Users/Počítač/Documents/catch-app" && npx jest --passWithNoTests --testPathPattern="NONEXISTENT" 2>&1 | grep -E "(PASS|FAIL|Tests:|jest)" | head -5</automated>
  </verify>
  <done>Jest beží bez syntax chýb. __mocks__ adresár existuje s auth.ts, firestore.ts, expo-localization.ts. jest.config.js existuje s jest-expo preset a moduleNameMapper pre @/ alias.</done>
</task>

<task type="auto">
  <name>Task 2: Stub testy pre auth + theme + i18n</name>
  <files>__tests__/unit/auth/auth.test.ts, __tests__/unit/auth/guard.test.ts, __tests__/unit/theme.test.ts, __tests__/unit/i18n.test.ts</files>
  <action>
Testy sú STUB — písané pred implementáciou. Budú RED kým nie sú implementované zodpovedajúce súbory. To je správne — TDD Red phase.

**__tests__/unit/auth/auth.test.ts:**
```typescript
/**
 * Auth unit testy
 * Pokrýva: AUTH-01 (register), AUTH-02 (email verif), AUTH-03 (reset), AUTH-06 (logout)
 *
 * STAV: RED — čaká na implementáciu app/(auth)/register.tsx, login.tsx, forgot-password.tsx
 */
import authModule, { mockUser, mockAuth } from '../../../__mocks__/@react-native-firebase/auth';

// Resetovať mocky pred každým testom
beforeEach(() => {
  jest.clearAllMocks();
});

describe('AUTH-01: Register s email a heslom', () => {
  it('createUserWithEmailAndPassword sa zavolá so správnymi parametrami', async () => {
    const auth = authModule();
    await auth.createUserWithEmailAndPassword('test@catchapp.sk', 'SecurePass123!');

    expect(auth.createUserWithEmailAndPassword).toHaveBeenCalledWith(
      'test@catchapp.sk',
      'SecurePass123!'
    );
  });

  it('createUserWithEmailAndPassword vráti user objekt s uid', async () => {
    const auth = authModule();
    const result = await auth.createUserWithEmailAndPassword('test@catchapp.sk', 'SecurePass123!');

    expect(result.user.uid).toBe('test-uid-123');
    expect(result.user.email).toBe('test@catchapp.sk');
  });
});

describe('AUTH-02: Email verifikácia po registrácii', () => {
  it('sendEmailVerification sa zavolá na user objekte po registrácii', async () => {
    const auth = authModule();
    const result = await auth.createUserWithEmailAndPassword('test@catchapp.sk', 'SecurePass123!');
    await result.user.sendEmailVerification();

    expect(result.user.sendEmailVerification).toHaveBeenCalledTimes(1);
  });
});

describe('AUTH-03: Reset hesla', () => {
  it('sendPasswordResetEmail sa zavolá s emailom', async () => {
    const auth = authModule();
    await auth.sendPasswordResetEmail('test@catchapp.sk');

    expect(auth.sendPasswordResetEmail).toHaveBeenCalledWith('test@catchapp.sk');
  });
});

describe('AUTH-06: Odhlásenie', () => {
  it('signOut sa zavolá bez argumentov', async () => {
    const auth = authModule();
    await auth.signOut();

    expect(auth.signOut).toHaveBeenCalledTimes(1);
  });

  it('signOut resolve bez chyby', async () => {
    const auth = authModule();
    await expect(auth.signOut()).resolves.toBeUndefined();
  });
});
```

**__tests__/unit/auth/guard.test.ts:**
```typescript
/**
 * Auth guard test
 * Pokrýva: AUTH-05 — neprihlásený používateľ je presmerovaný na login
 *
 * STAV: RED — čaká na implementáciu app/_layout.tsx so Stack.Protected
 */

describe('AUTH-05: Stack.Protected auth guard', () => {
  it('guard prop je false keď user je null', () => {
    const user = null;
    const guard = !!user;
    expect(guard).toBe(false);
  });

  it('guard prop je true keď user je prihlásený', () => {
    const user = { uid: 'test-uid', email: 'test@test.sk' };
    const guard = !!user;
    expect(guard).toBe(true);
  });

  it('initialized flag je false pred onAuthStateChanged callback', () => {
    let initialized = false;
    // Simulovať Zustand auth store initial state
    const authStore = { user: null, initialized };
    expect(authStore.initialized).toBe(false);
  });
});
```

**__tests__/unit/theme.test.ts:**
```typescript
/**
 * Theme design system test
 * Pokrýva: INFRA-04 — theme.ts exportuje správne tokeny
 *
 * STAV: RED — čaká na implementáciu src/theme/theme.ts
 */

// Importovať theme — bude failovať kým theme.ts neexistuje
let theme: any;
try {
  theme = require('../../src/theme/theme').theme;
} catch {
  theme = null;
}

describe('INFRA-04: Design system theme tokeny', () => {
  it('theme.ts existuje a exportuje theme objekt', () => {
    expect(theme).not.toBeNull();
    expect(typeof theme).toBe('object');
  });

  it('theme.colors obsahuje všetky požadované farby', () => {
    if (!theme) return; // Skip ak theme.ts ešte neexistuje
    expect(theme.colors.bg).toBe('#0A1628');
    expect(theme.colors.primaryMid).toBe('#40916C');
    expect(theme.colors.accent).toBe('#F4A261');
    expect(theme.colors.surface).toBe('#112240');
    expect(theme.colors.surfaceHigh).toBe('#1A2F52');
    expect(theme.colors.primary).toBe('#1B4332');
  });

  it('theme.typography obsahuje Outfit, Inter, JetBrainsMono font families', () => {
    if (!theme) return;
    expect(theme.typography.heading.fontFamily).toBe('Outfit-Bold');
    expect(theme.typography.body.fontFamily).toBe('Inter-Regular');
    expect(theme.typography.mono.fontFamily).toBe('JetBrainsMono-Regular');
  });

  it('theme.spacing obsahuje xs, sm, md, lg, xl, xxl', () => {
    if (!theme) return;
    expect(theme.spacing.xs).toBe(4);
    expect(theme.spacing.sm).toBe(8);
    expect(theme.spacing.md).toBe(16);
    expect(theme.spacing.lg).toBe(24);
  });

  it('theme.radius obsahuje sm, md, lg, xl, full', () => {
    if (!theme) return;
    expect(theme.radius.full).toBe(999);
    expect(typeof theme.radius.md).toBe('number');
  });
});
```

**__tests__/unit/i18n.test.ts:**
```typescript
/**
 * i18n test
 * Pokrýva: INFRA-03 — SK je default jazyk, fallback na SK
 *
 * STAV: RED — čaká na implementáciu src/lib/i18n.ts a src/locales/sk.json
 */

let i18n: any;
try {
  i18n = require('../../src/lib/i18n').i18n;
} catch {
  i18n = null;
}

describe('INFRA-03: i18n konfigurácia', () => {
  it('i18n modul existuje a je exportovaný', () => {
    expect(i18n).not.toBeNull();
  });

  it('default locale je sk', () => {
    if (!i18n) return;
    expect(i18n.defaultLocale).toBe('sk');
  });

  it('fallback je povolený', () => {
    if (!i18n) return;
    expect(i18n.enableFallback).toBe(true);
  });

  it('SK locale obsahuje základné kľúče', () => {
    if (!i18n) return;
    // Overiť že SK translations sú načítané
    const translations = i18n.translations;
    expect(translations).toBeDefined();
    expect(translations.sk).toBeDefined();
  });
});
```
  </action>
  <verify>
    <automated>cd "C:/Users/Počítač/Documents/catch-app" && npx jest --testPathPattern="__tests__/unit/auth/auth.test" --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>Všetky 4 stub test súbory existujú. npx jest --passWithNoTests beží bez fatal chýb (niektoré testy môžu byť RED — to je správne).</done>
</task>

</tasks>

<verification>
```bash
cd "C:/Users/Počítač/Documents/catch-app"
npx jest --passWithNoTests 2>&1 | tail -15
```
Jest beží, Firebase mocks sú načítané, stub testy existujú.
</verification>

<success_criteria>
- jest.config.js s jest-expo preset a moduleNameMapper pre @/ alias
- __mocks__/@react-native-firebase/auth.ts s plnou mock implementáciou
- __mocks__/@react-native-firebase/firestore.ts s mock kolekciami
- __mocks__/expo-localization.ts s SK locale
- 4 stub test súbory pokrývajúce AUTH-01, AUTH-02, AUTH-03, AUTH-05, AUTH-06, INFRA-03, INFRA-04
- npx jest --passWithNoTests beží bez fatal errors
</success_criteria>

<output>
Po dokončení vytvoriť `.planning/phases/01-foundation-auth/01-02-SUMMARY.md` s:
- Jest verzia a konfigurácia
- Zoznam mockovaných modulov
- Počet stub testov a ich stav (RED/PASS)
</output>
