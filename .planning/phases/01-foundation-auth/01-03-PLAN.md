---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/theme/theme.ts
  - src/lib/i18n.ts
  - src/locales/sk.json
  - src/locales/cs.json
  - src/components/ui/Button.tsx
  - src/components/ui/Card.tsx
  - src/components/ui/Badge.tsx
  - src/components/ui/Avatar.tsx
  - src/components/ui/TextInput.tsx
  - src/components/ui/Skeleton.tsx
autonomous: true
requirements:
  - INFRA-03
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "theme.ts exportuje farby, typografiu, spacing, radius a shadow tokeny"
    - "i18n.ts je nakonfigurovaný so SK ako default a fallback jazykom"
    - "6 UI primitives (Button, Card, Badge, Avatar, TextInput, Skeleton) sú použiteľné"
    - "Všetky komponenty používajú theme tokeny — žiadne hardcoded farby"
    - "npx jest theme a npx jest i18n sú GREEN"
  artifacts:
    - path: "src/theme/theme.ts"
      provides: "Farby (#0A1628, #40916C, #F4A261...), typografia (Outfit/Inter/JetBrainsMono), spacing, radius, shadow tokeny"
      exports: ["theme"]
    - path: "src/lib/i18n.ts"
      provides: "i18n-js konfigurácia, SK default, CZ fallback"
      exports: ["i18n"]
    - path: "src/locales/sk.json"
      provides: "Slovenské preklady pre auth flow, tab labels, common UI strings"
    - path: "src/components/ui/Button.tsx"
      provides: "Reusable Button s primary/secondary/ghost variantmi, loading state, disabled state"
      exports: ["Button"]
    - path: "src/components/ui/TextInput.tsx"
      provides: "TextInput s error state, label, helper text — používaný v auth formoch"
      exports: ["AppTextInput"]
    - path: "src/components/ui/Skeleton.tsx"
      provides: "Animated skeleton loader pre placeholder screens"
      exports: ["Skeleton"]
  key_links:
    - from: "src/components/ui/Button.tsx"
      to: "src/theme/theme.ts"
      via: "import { theme } from '@/theme/theme'"
      pattern: "theme\\.colors\\."
    - from: "src/lib/i18n.ts"
      to: "src/locales/sk.json"
      via: "import sk from '@/locales/sk.json'"
      pattern: "import.*sk\\.json"
---

<objective>
Vytvoriť design system (theme.ts), i18n konfiguráciu a základné UI primitives. Toto je shared foundation — každý screen v Phase 1–8 bude importovať theme.ts a používať tieto komponenty.

Purpose: Bez theme.ts a UI primitives nemožno implementovať žiadne screeny. Design system určuje vizuálnu identitu celej appky.
Output: theme.ts s tokenmi, i18n setup s SK lokalizáciou, 6 reusable UI komponentov (Button, Card, Badge, Avatar, TextInput, Skeleton).
</objective>

<execution_context>
@C:/Users/Počítač/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Počítač/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Počítač/Documents/catch-app/.planning/phases/01-foundation-auth/01-CONTEXT.md
@C:/Users/Počítač/Documents/catch-app/.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<interfaces>
<!-- Kľúčové konvencie pre tento plan -->
<!-- Všetky komponenty MUSIA importovať theme z '@/theme/theme' -->
<!-- Žiadne hardcoded farby, spacing ani font sizes v komponentoch -->

Farby (z locked decisions):
- bg: '#0A1628' navy
- surface: '#112240'
- surfaceHigh: '#1A2F52'
- primary: '#1B4332' tmavá zelená
- primaryMid: '#40916C' stredná zelená (active state, CTA)
- accent: '#F4A261' oranžová (FAB, primary CTA button)
- textPrimary: '#F8F9FA'
- textSecondary: '#9CA3AF'
- textMuted: '#6B7280'
- error: '#EF4444'
- success: '#22C55E'
- tabActive: '#40916C'
- tabInactive: '#6B7280'

Fonty (z locked decisions):
- Outfit-Bold, Outfit-SemiBold → headings
- Inter-Regular, Inter-Medium → body text
- JetBrainsMono-Regular → čísla, štatistiky
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: theme.ts + i18n konfigurácia</name>
  <files>src/theme/theme.ts, src/lib/i18n.ts, src/locales/sk.json, src/locales/cs.json</files>
  <behavior>
    - theme.colors.bg === '#0A1628'
    - theme.colors.primaryMid === '#40916C'
    - theme.colors.accent === '#F4A261'
    - theme.typography.heading.fontFamily === 'Outfit-Bold'
    - theme.typography.mono.fontFamily === 'JetBrainsMono-Regular'
    - theme.spacing.md === 16
    - theme.radius.full === 999
    - i18n.defaultLocale === 'sk'
    - i18n.enableFallback === true
    - i18n.translations.sk je definované
  </behavior>
  <action>
**Krok 1 — src/theme/theme.ts** (implementovať podľa locked decisions):
```typescript
export const theme = {
  colors: {
    // Backgrounds
    bg: '#0A1628',
    surface: '#112240',
    surfaceHigh: '#1A2F52',

    // Brand greens
    primary: '#1B4332',
    primaryMid: '#40916C',

    // Accent
    accent: '#F4A261',
    accentBlue: '#1E6091',

    // Text
    textPrimary: '#F8F9FA',
    textSecondary: '#9CA3AF',
    textMuted: '#6B7280',

    // Semantic
    error: '#EF4444',
    errorSurface: 'rgba(239,68,68,0.1)',
    success: '#22C55E',
    warning: '#F59E0B',

    // Tab bar
    tabActive: '#40916C',
    tabInactive: '#6B7280',
    tabBar: '#0A1628',
    tabBorder: '#1A2F52',

    // Dividers
    divider: '#1A2F52',
    overlay: 'rgba(0,0,0,0.6)',
  },

  typography: {
    // Headings — Outfit
    heading: { fontFamily: 'Outfit-Bold', fontSize: 24, lineHeight: 30 },
    headingLg: { fontFamily: 'Outfit-Bold', fontSize: 28, lineHeight: 36 },
    headingSemi: { fontFamily: 'Outfit-SemiBold', fontSize: 20, lineHeight: 26 },
    headingSm: { fontFamily: 'Outfit-SemiBold', fontSize: 16, lineHeight: 22 },

    // Body — Inter
    body: { fontFamily: 'Inter-Regular', fontSize: 15, lineHeight: 22 },
    bodyMedium: { fontFamily: 'Inter-Medium', fontSize: 15, lineHeight: 22 },
    bodySm: { fontFamily: 'Inter-Regular', fontSize: 13, lineHeight: 18 },
    bodySmMedium: { fontFamily: 'Inter-Medium', fontSize: 13, lineHeight: 18 },
    caption: { fontFamily: 'Inter-Regular', fontSize: 11, lineHeight: 16 },

    // Mono — JetBrains Mono (čísla, štatistiky)
    mono: { fontFamily: 'JetBrainsMono-Regular', fontSize: 14, lineHeight: 20 },
    monoLg: { fontFamily: 'JetBrainsMono-Regular', fontSize: 20, lineHeight: 26 },
    monoSm: { fontFamily: 'JetBrainsMono-Regular', fontSize: 12, lineHeight: 16 },
  },

  spacing: {
    xs: 4,
    sm: 8,
    md: 16,
    lg: 24,
    xl: 32,
    xxl: 48,
    xxxl: 64,
  },

  radius: {
    xs: 4,
    sm: 8,
    md: 12,
    lg: 16,
    xl: 24,
    full: 999,
  },

  shadow: {
    sm: {
      shadowColor: '#000',
      shadowOffset: { width: 0, height: 1 },
      shadowOpacity: 0.3,
      shadowRadius: 3,
      elevation: 2,
    },
    md: {
      shadowColor: '#000',
      shadowOffset: { width: 0, height: 4 },
      shadowOpacity: 0.4,
      shadowRadius: 12,
      elevation: 6,
    },
    accent: {
      shadowColor: '#F4A261',
      shadowOffset: { width: 0, height: 4 },
      shadowOpacity: 0.4,
      shadowRadius: 12,
      elevation: 8,
    },
  },
} as const;

export type Theme = typeof theme;
export type ThemeColors = typeof theme.colors;
```

**Krok 2 — src/locales/sk.json** (základné SK stringy pre Phase 1):
```json
{
  "auth": {
    "login": "Prihlásiť sa",
    "register": "Registrovať sa",
    "logout": "Odhlásiť sa",
    "email": "Email",
    "password": "Heslo",
    "confirmPassword": "Potvrdiť heslo",
    "forgotPassword": "Zabudol si heslo?",
    "resetPassword": "Obnoviť heslo",
    "noAccount": "Nemáš účet?",
    "hasAccount": "Máš účet?",
    "registerHere": "Registruj sa",
    "loginHere": "Prihlás sa",
    "emailSent": "Email odoslaný",
    "checkInbox": "Skontroluj svoju schránku",
    "verifyEmail": "Overiť email",
    "verifyEmailDesc": "Poslali sme ti verifikačný email. Klikni na odkaz v emaili.",
    "errors": {
      "emailRequired": "Email je povinný",
      "passwordRequired": "Heslo je povinné",
      "passwordMismatch": "Heslá sa nezhodujú",
      "passwordTooShort": "Heslo musí mať aspoň 8 znakov",
      "invalidEmail": "Neplatný email",
      "invalidCredential": "Nesprávny email alebo heslo",
      "tooManyRequests": "Príliš veľa pokusov. Skúste neskôr.",
      "networkError": "Chyba siete. Skontroluj pripojenie.",
      "unknown": "Nastala chyba. Skúste znova."
    }
  },
  "tabs": {
    "podmienky": "Podmienky",
    "feed": "Feed",
    "pridat": "Pridať",
    "dennik": "Denník",
    "bazar": "Bazár"
  },
  "common": {
    "loading": "Načítavanie…",
    "save": "Uložiť",
    "cancel": "Zrušiť",
    "delete": "Zmazať",
    "edit": "Upraviť",
    "back": "Späť",
    "next": "Ďalej",
    "done": "Hotovo",
    "error": "Chyba",
    "retry": "Skúsiť znova",
    "comingSoon": "Čoskoro",
    "noData": "Žiadne dáta"
  },
  "placeholders": {
    "podmienky": {
      "title": "Podmienky",
      "subtitle": "Live počasie a AI tip na rybolov"
    },
    "feed": {
      "title": "Feed",
      "subtitle": "Úlovky komunity"
    },
    "dennik": {
      "title": "Denník",
      "subtitle": "Moje úlovky"
    },
    "bazar": {
      "title": "Bazár",
      "subtitle": "Rybárske vybavenie"
    }
  }
}
```

**Krok 3 — src/locales/cs.json** (CZ preklady — kópia SK s CZ variantmi):
```json
{
  "auth": {
    "login": "Přihlásit se",
    "register": "Registrovat se",
    "logout": "Odhlásit se",
    "email": "Email",
    "password": "Heslo",
    "confirmPassword": "Potvrdit heslo",
    "forgotPassword": "Zapomněl jsi heslo?",
    "resetPassword": "Obnovit heslo",
    "noAccount": "Nemáš účet?",
    "hasAccount": "Máš účet?",
    "registerHere": "Registruj se",
    "loginHere": "Přihlas se",
    "emailSent": "Email odeslán",
    "checkInbox": "Zkontroluj svou schránku",
    "verifyEmail": "Ověřit email",
    "verifyEmailDesc": "Poslali jsme ti ověřovací email. Klikni na odkaz v emailu.",
    "errors": {
      "emailRequired": "Email je povinný",
      "passwordRequired": "Heslo je povinné",
      "passwordMismatch": "Hesla se neshodují",
      "passwordTooShort": "Heslo musí mít alespoň 8 znaků",
      "invalidEmail": "Neplatný email",
      "invalidCredential": "Nesprávný email nebo heslo",
      "tooManyRequests": "Příliš mnoho pokusů. Zkuste později.",
      "networkError": "Chyba sítě. Zkontroluj připojení.",
      "unknown": "Nastala chyba. Zkuste znova."
    }
  },
  "tabs": {
    "podmienky": "Podmínky",
    "feed": "Feed",
    "pridat": "Přidat",
    "dennik": "Deník",
    "bazar": "Bazar"
  },
  "common": {
    "loading": "Načítávání…",
    "save": "Uložit",
    "cancel": "Zrušit",
    "delete": "Smazat",
    "edit": "Upravit",
    "back": "Zpět",
    "next": "Dále",
    "done": "Hotovo",
    "error": "Chyba",
    "retry": "Zkusit znova",
    "comingSoon": "Brzy",
    "noData": "Žádná data"
  },
  "placeholders": {
    "podmienky": { "title": "Podmínky", "subtitle": "Live počasí a AI tip na rybolov" },
    "feed": { "title": "Feed", "subtitle": "Úlovky komunity" },
    "dennik": { "title": "Deník", "subtitle": "Moje úlovky" },
    "bazar": { "title": "Bazar", "subtitle": "Rybářské vybavení" }
  }
}
```

**Krok 4 — src/lib/i18n.ts:**
```typescript
import { I18n } from 'i18n-js';
import * as Localization from 'expo-localization';
import sk from '@/locales/sk.json';
import cs from '@/locales/cs.json';

export const i18n = new I18n({ sk, cs });

// SK ako default, fallback na SK pre neznáme lokalizácie
const deviceLocale = Localization.getLocales()[0]?.languageCode ?? 'sk';
i18n.locale = ['sk', 'cs'].includes(deviceLocale) ? deviceLocale : 'sk';
i18n.enableFallback = true;
i18n.defaultLocale = 'sk';
```

Spustiť testy po implementácii:
```bash
cd "C:/Users/Počítač/Documents/catch-app"
npx jest --testPathPattern="theme|i18n" 2>&1 | tail -20
```
  </action>
  <verify>
    <automated>cd "C:/Users/Počítač/Documents/catch-app" && npx jest --testPathPattern="__tests__/unit/theme|__tests__/unit/i18n" 2>&1 | tail -15</automated>
  </verify>
  <done>npx jest theme a npx jest i18n sú GREEN (všetky testy PASS). theme.ts a i18n.ts existujú s požadovanými exportmi. sk.json a cs.json majú auth, tabs, common a placeholders kľúče.</done>
</task>

<task type="auto">
  <name>Task 2: UI primitives — Button, Card, Badge, Avatar, TextInput, Skeleton</name>
  <files>src/components/ui/Button.tsx, src/components/ui/Card.tsx, src/components/ui/Badge.tsx, src/components/ui/Avatar.tsx, src/components/ui/TextInput.tsx, src/components/ui/Skeleton.tsx, src/components/ui/index.ts</files>
  <action>
Všetky komponenty MUSIA:
- Importovať `theme` z `@/theme/theme` — žiadne hardcoded farby/spacing
- Používať `Pressable` namiesto `TouchableOpacity`
- Podporovať dark-only design (žiadny light mode)
- Mať TypeScript props interface

**src/components/ui/Button.tsx:**
```typescript
import React from 'react';
import { Pressable, Text, ActivityIndicator, StyleSheet, PressableProps } from 'react-native';
import { theme } from '@/theme/theme';

type ButtonVariant = 'primary' | 'secondary' | 'ghost' | 'danger';
type ButtonSize = 'sm' | 'md' | 'lg';

interface ButtonProps extends Omit<PressableProps, 'style'> {
  label: string;
  variant?: ButtonVariant;
  size?: ButtonSize;
  loading?: boolean;
  fullWidth?: boolean;
}

export function Button({
  label,
  variant = 'primary',
  size = 'md',
  loading = false,
  fullWidth = false,
  disabled,
  ...props
}: ButtonProps) {
  const isDisabled = disabled || loading;

  return (
    <Pressable
      {...props}
      disabled={isDisabled}
      style={({ pressed }) => [
        styles.base,
        styles[variant],
        styles[`size_${size}`],
        fullWidth && styles.fullWidth,
        pressed && styles.pressed,
        isDisabled && styles.disabled,
      ]}
    >
      {loading ? (
        <ActivityIndicator
          size="small"
          color={variant === 'primary' ? theme.colors.bg : theme.colors.textPrimary}
        />
      ) : (
        <Text style={[styles.label, styles[`label_${variant}`], styles[`labelSize_${size}`]]}>
          {label}
        </Text>
      )}
    </Pressable>
  );
}

const styles = StyleSheet.create({
  base: {
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: theme.radius.md,
    borderCurve: 'continuous',
  } as any,
  // Varianty
  primary: { backgroundColor: theme.colors.accent },
  secondary: { backgroundColor: theme.colors.surface, borderWidth: 1, borderColor: theme.colors.surfaceHigh },
  ghost: { backgroundColor: 'transparent' },
  danger: { backgroundColor: 'rgba(239,68,68,0.15)', borderWidth: 1, borderColor: theme.colors.error },
  // Veľkosti
  size_sm: { paddingVertical: theme.spacing.xs, paddingHorizontal: theme.spacing.md, minHeight: 36 },
  size_md: { paddingVertical: theme.spacing.sm + 4, paddingHorizontal: theme.spacing.lg, minHeight: 48 },
  size_lg: { paddingVertical: theme.spacing.md, paddingHorizontal: theme.spacing.xl, minHeight: 56 },
  // Stavy
  fullWidth: { width: '100%' },
  pressed: { opacity: 0.8 },
  disabled: { opacity: 0.4 },
  // Labely
  label: { ...theme.typography.bodyMedium },
  label_primary: { color: theme.colors.bg },
  label_secondary: { color: theme.colors.textPrimary },
  label_ghost: { color: theme.colors.primaryMid },
  label_danger: { color: theme.colors.error },
  labelSize_sm: { fontSize: 13 },
  labelSize_md: { fontSize: 15 },
  labelSize_lg: { fontSize: 16 },
});
```

**src/components/ui/TextInput.tsx** (pre auth formy — inline error state):
```typescript
import React, { useState } from 'react';
import {
  View,
  TextInput as RNTextInput,
  Text,
  StyleSheet,
  TextInputProps as RNTextInputProps,
} from 'react-native';
import { theme } from '@/theme/theme';

interface AppTextInputProps extends RNTextInputProps {
  label?: string;
  error?: string;
  helper?: string;
}

export function AppTextInput({ label, error, helper, style, ...props }: AppTextInputProps) {
  const [focused, setFocused] = useState(false);

  return (
    <View style={styles.container}>
      {label && <Text style={styles.label}>{label}</Text>}
      <RNTextInput
        {...props}
        style={[
          styles.input,
          focused && styles.inputFocused,
          error && styles.inputError,
          style,
        ]}
        placeholderTextColor={theme.colors.textMuted}
        onFocus={(e) => { setFocused(true); props.onFocus?.(e); }}
        onBlur={(e) => { setFocused(false); props.onBlur?.(e); }}
      />
      {error && <Text style={styles.errorText}>{error}</Text>}
      {helper && !error && <Text style={styles.helperText}>{helper}</Text>}
    </View>
  );
}

const styles = StyleSheet.create({
  container: { gap: theme.spacing.xs },
  label: { ...theme.typography.bodySmMedium, color: theme.colors.textSecondary },
  input: {
    backgroundColor: theme.colors.surface,
    borderRadius: theme.radius.md,
    borderCurve: 'continuous',
    borderWidth: 1,
    borderColor: theme.colors.surfaceHigh,
    paddingHorizontal: theme.spacing.md,
    paddingVertical: theme.spacing.sm + 4,
    color: theme.colors.textPrimary,
    ...theme.typography.body,
    minHeight: 48,
  } as any,
  inputFocused: { borderColor: theme.colors.primaryMid },
  inputError: { borderColor: theme.colors.error },
  errorText: { ...theme.typography.bodySm, color: theme.colors.error },
  helperText: { ...theme.typography.bodySm, color: theme.colors.textMuted },
});
```

**src/components/ui/Card.tsx:**
```typescript
import React from 'react';
import { View, StyleSheet, ViewProps } from 'react-native';
import { theme } from '@/theme/theme';

interface CardProps extends ViewProps {
  elevated?: boolean;
  children: React.ReactNode;
}

export function Card({ elevated = false, style, children, ...props }: CardProps) {
  return (
    <View
      {...props}
      style={[styles.card, elevated && styles.elevated, style]}
    >
      {children}
    </View>
  );
}

const styles = StyleSheet.create({
  card: {
    backgroundColor: theme.colors.surface,
    borderRadius: theme.radius.lg,
    borderCurve: 'continuous',
    padding: theme.spacing.md,
    borderWidth: 1,
    borderColor: theme.colors.surfaceHigh,
  } as any,
  elevated: theme.shadow.md as any,
});
```

**src/components/ui/Badge.tsx:**
```typescript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { theme } from '@/theme/theme';

type BadgeVariant = 'primary' | 'accent' | 'success' | 'error' | 'neutral';

interface BadgeProps {
  label: string;
  variant?: BadgeVariant;
  size?: 'sm' | 'md';
}

export function Badge({ label, variant = 'neutral', size = 'md' }: BadgeProps) {
  return (
    <View style={[styles.base, styles[variant], size === 'sm' && styles.small]}>
      <Text style={[styles.label, styles[`label_${variant}`], size === 'sm' && styles.labelSmall]}>
        {label}
      </Text>
    </View>
  );
}

const styles = StyleSheet.create({
  base: {
    paddingHorizontal: theme.spacing.sm,
    paddingVertical: 3,
    borderRadius: theme.radius.full,
    alignSelf: 'flex-start',
  },
  small: { paddingHorizontal: 6, paddingVertical: 2 },
  primary: { backgroundColor: `${theme.colors.primaryMid}20` },
  accent: { backgroundColor: `${theme.colors.accent}20` },
  success: { backgroundColor: 'rgba(34,197,94,0.15)' },
  error: { backgroundColor: theme.colors.errorSurface },
  neutral: { backgroundColor: theme.colors.surfaceHigh },
  label: { ...theme.typography.caption },
  label_primary: { color: theme.colors.primaryMid },
  label_accent: { color: theme.colors.accent },
  label_success: { color: theme.colors.success },
  label_error: { color: theme.colors.error },
  label_neutral: { color: theme.colors.textMuted },
  labelSmall: { fontSize: 10 },
});
```

**src/components/ui/Avatar.tsx:**
```typescript
import React from 'react';
import { View, Text, Image, StyleSheet } from 'react-native';
import { theme } from '@/theme/theme';

interface AvatarProps {
  uri?: string | null;
  name?: string;
  size?: number;
}

function getInitials(name?: string): string {
  if (!name) return '?';
  return name.split(' ').map(w => w[0]).join('').toUpperCase().slice(0, 2);
}

export function Avatar({ uri, name, size = 40 }: AvatarProps) {
  const style = { width: size, height: size, borderRadius: size / 2 };

  if (uri) {
    return <Image source={{ uri }} style={[styles.base, style]} />;
  }

  return (
    <View style={[styles.base, styles.fallback, style]}>
      <Text style={[styles.initials, { fontSize: size * 0.35 }]}>
        {getInitials(name)}
      </Text>
    </View>
  );
}

const styles = StyleSheet.create({
  base: { overflow: 'hidden' },
  fallback: { backgroundColor: theme.colors.primary, alignItems: 'center', justifyContent: 'center' },
  initials: { color: theme.colors.primaryMid, ...theme.typography.bodyMedium },
});
```

**src/components/ui/Skeleton.tsx** (animovaný loader):
```typescript
import React, { useEffect, useRef } from 'react';
import { Animated, View, StyleSheet, ViewStyle } from 'react-native';
import { theme } from '@/theme/theme';

interface SkeletonProps {
  width?: number | string;
  height?: number;
  borderRadius?: number;
  style?: ViewStyle;
}

export function Skeleton({ width = '100%', height = 16, borderRadius = theme.radius.sm, style }: SkeletonProps) {
  const opacity = useRef(new Animated.Value(0.3)).current;

  useEffect(() => {
    const animation = Animated.loop(
      Animated.sequence([
        Animated.timing(opacity, { toValue: 0.8, duration: 800, useNativeDriver: true }),
        Animated.timing(opacity, { toValue: 0.3, duration: 800, useNativeDriver: true }),
      ])
    );
    animation.start();
    return () => animation.stop();
  }, [opacity]);

  return (
    <Animated.View
      style={[
        styles.skeleton,
        { width: width as any, height, borderRadius, opacity },
        style,
      ]}
    />
  );
}

const styles = StyleSheet.create({
  skeleton: {
    backgroundColor: theme.colors.surfaceHigh,
  },
});
```

**src/components/ui/index.ts** (barrel exports):
```typescript
export { Button } from './Button';
export { Card } from './Card';
export { Badge } from './Badge';
export { Avatar } from './Avatar';
export { AppTextInput } from './TextInput';
export { Skeleton } from './Skeleton';
```
  </action>
  <verify>
    <automated>cd "C:/Users/Počítač/Documents/catch-app" && node -e "try { require('./src/components/ui/index.ts'); console.log('ERROR: ts-node needed'); } catch(e) { if (e.code === 'MODULE_NOT_FOUND') console.log('MODULE_NOT_FOUND — check path'); else console.log('TypeScript OK — file exists:', e.message.includes('index.ts') ? 'yes' : 'no'); }" && ls src/components/ui/