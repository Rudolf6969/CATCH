---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app.json
  - eas.json
  - metro.config.js
  - google-services.json
  - GoogleService-Info.plist
  - src/lib/firebase.ts
  - firestore.rules
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-06

must_haves:
  truths:
    - "Expo SDK 53 projekt sa spustí cez Metro bundler bez chýb (npx expo start)"
    - "react-native-firebase je nainštalovaný a nakonfigurovaný v app.json pluginoch"
    - "EAS profiles (development, preview, production) sú definované v eas.json"
    - "Firestore skeleton security rules sú nasadené"
  artifacts:
    - path: "package.json"
      provides: "Všetky závislosti nainštalované (expo 53, @react-native-firebase/*, zustand, @tanstack/react-query)"
    - path: "app.json"
      provides: "Firebase pluginy, expo-build-properties iOS useFrameworks:static, bundle ID sk.catchapp.app"
    - path: "eas.json"
      provides: "development (APK, developmentClient), preview, production profiles"
    - path: "metro.config.js"
      provides: "Metro config bez unstable_enablePackageExports (natívny SDK nepotrebuje)"
    - path: "src/lib/firebase.ts"
      provides: "Centralizované importy auth/firestore/storage z natívneho SDK"
    - path: "firestore.rules"
      provides: "Skeleton security rules pre users, catches, spots kolekcie"
  key_links:
    - from: "app.json plugins"
      to: "@react-native-firebase/app"
      via: "config plugin entry"
      pattern: "\"@react-native-firebase/app\""
    - from: "app.json"
      to: "google-services.json"
      via: "android.googleServicesFile"
      pattern: "googleServicesFile"
---

<objective>
Inicializovať Expo SDK 53 greenfield projekt s react-native-firebase (natívny SDK) a nakonfigurovať EAS Build profily. Toto je foundation — každá ďalšia fáza závisí od tejto infraštruktúry.

Purpose: Bez funkčného Expo projektu s Firebase a EAS konfiguráciou nie je možné spustiť žiadnu ďalšiu prácu. Windows longpaths musia byť ošetrené od začiatku.
Output: Expo projekt s nainštalovanými závislosti, Firebase konfiguráciou, EAS profilmi a skeleton Firestore rules.
</objective>

<execution_context>
@C:/Users/Počítač/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Počítač/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Počítač/Documents/catch-app/.planning/PROJECT.md
@C:/Users/Počítač/Documents/catch-app/.planning/ROADMAP.md
@C:/Users/Počítač/Documents/catch-app/.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expo SDK 53 projekt init + Windows setup</name>
  <files>package.json, metro.config.js, tsconfig.json, .gitignore</files>
  <action>
Inicializovať Expo SDK 53 projekt v C:/Users/Počítač/Documents/catch-app.

**Krok 1 — Windows longpaths (ak ešte nenastavené):**
```powershell
git config --global core.longpaths true
```

**Krok 2 — Projekt existuje ako greenfield — inštalovať závislosti:**

Skontroluj či package.json existuje. Ak nie, spusti:
```bash
cd C:/Users/Počítač/Documents/catch-app
npx create-expo-app@latest . --template blank-typescript
```

Ak package.json existuje, pokračuj priamo na inštaláciu závislostí.

**Krok 3 — Firebase natívny SDK:**
```bash
npx expo install @react-native-firebase/app @react-native-firebase/auth @react-native-firebase/firestore @react-native-firebase/storage @react-native-firebase/messaging
```

**Krok 4 — Expo utilities:**
```bash
npx expo install expo-dev-client expo-build-properties expo-font expo-splash-screen expo-localization expo-file-system expo-application expo-device
```

**Krok 5 — State management + i18n:**
```bash
npm install zustand @tanstack/react-query i18n-js posthog-react-native
npm install --save-dev @types/i18n-js
```

**Krok 6 — UI + animácie:**
```bash
npx expo install react-native-safe-area-context react-native-reanimated @expo/vector-icons
```

**Krok 7 — metro.config.js** (použiť natívny SDK, package.json exports zostatok):
```javascript
const { getDefaultConfig } = require('expo/metro-config');

const config = getDefaultConfig(__dirname);

module.exports = config;
```
Poznámka: NEPRIDÁVAŤ `unstable_enablePackageExports: false` — natívny SDK tento problém nemá. Metro default je správny.

**Krok 8 — package.json overrides pre React 19 peer deps:**
Pridať do package.json:
```json
"overrides": {
  "react": "19.0.0"
}
```

**Krok 9 — Environment variable pre Metro performance:**
Pridať do `.env`:
```
EXPO_USE_FAST_RESOLVER=1
```
  </action>
  <verify>
    <automated>cd "C:/Users/Počítač/Documents/catch-app" && node -e "const p = require('./package.json'); console.log('expo:', p.dependencies.expo, '| firebase:', p.dependencies['@react-native-firebase/app'], '| zustand:', p.dependencies.zustand)"</automated>
  </verify>
  <done>package.json obsahuje expo 53.x, @react-native-firebase/app 21.x+, zustand, @tanstack/react-query. metro.config.js existuje. .env obsahuje EXPO_USE_FAST_RESOLVER=1.</done>
</task>

<task type="auto">
  <name>Task 2: Firebase konfigurácia + app.json + EAS profily</name>
  <files>app.json, eas.json, src/lib/firebase.ts, google-services.json, GoogleService-Info.plist, firestore.rules</files>
  <action>
**Krok 1 — app.json konfigurácia:**

Aktualizovať app.json s Firebase pluginmi, bundle ID a font pluginom:
```json
{
  "expo": {
    "name": "CATCH",
    "slug": "catch-app",
    "version": "1.0.0",
    "orientation": "portrait",
    "icon": "./assets/images/icon.png",
    "scheme": "catchapp",
    "userInterfaceStyle": "dark",
    "newArchEnabled": true,
    "ios": {
      "supportsTablet": false,
      "bundleIdentifier": "sk.catchapp.app",
      "googleServicesFile": "./GoogleService-Info.plist"
    },
    "android": {
      "adaptiveIcon": {
        "foregroundImage": "./assets/images/adaptive-icon.png",
        "backgroundColor": "#0A1628"
      },
      "package": "sk.catchapp.app",
      "googleServicesFile": "./google-services.json"
    },
    "plugins": [
      "@react-native-firebase/app",
      [
        "expo-build-properties",
        {
          "ios": { "useFrameworks": "static" }
        }
      ],
      [
        "expo-font",
        {
          "fonts": [
            "./assets/fonts/Outfit-Regular.ttf",
            "./assets/fonts/Outfit-SemiBold.ttf",
            "./assets/fonts/Outfit-Bold.ttf",
            "./assets/fonts/Inter-Regular.ttf",
            "./assets/fonts/Inter-Medium.ttf",
            "./assets/fonts/JetBrainsMono-Regular.ttf"
          ]
        }
      ]
    ]
  }
}
```

**Krok 2 — eas.json:**
```json
{
  "cli": { "version": ">= 16.0.0" },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "android": { "buildType": "apk" },
      "ios": { "simulator": false },
      "env": {
        "EXPO_USE_FAST_RESOLVER": "1"
      }
    },
    "preview": {
      "distribution": "internal",
      "android": { "buildType": "apk" }
    },
    "production": {
      "autoIncrement": true
    }
  },
  "submit": {
    "production": {}
  }
}
```

**Krok 3 — src/lib/firebase.ts** (centralizované importy, žiadny initializeApp):
```typescript
// react-native-firebase sa auto-inicializuje z google-services.json / GoogleService-Info.plist
// ŽIADNY initializeApp() call — natívny SDK robí init automaticky
import auth from '@react-native-firebase/auth';
import firestore from '@react-native-firebase/firestore';
import storage from '@react-native-firebase/storage';

export { auth, firestore, storage };
```

**Krok 4 — Placeholder Firebase config súbory:**

Vytvoriť `google-services.json` placeholder (developer ho nahradí reálnym zo Firebase Console):
```json
{
  "_PLACEHOLDER": "NAHRADIŤ reálnym google-services.json z Firebase Console → Project Settings → Android app",
  "project_info": {
    "project_id": "catch-app-placeholder"
  },
  "client": []
}
```

Vytvoriť `GoogleService-Info.plist` placeholder:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- NAHRADIŤ reálnym GoogleService-Info.plist z Firebase Console → Project Settings → iOS app -->
  <key>PLACEHOLDER</key>
  <string>true</string>
</dict>
</plist>
```

**Krok 5 — Firestore skeleton security rules:**
```
// firestore.rules
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Helper funkcie
    function isAuth() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isAuth() && request.auth.uid == userId;
    }

    // Users — read public, write vlastný profil
    match /users/{userId} {
      allow read: if isAuth();
      allow create: if isOwner(userId);
      allow update: if isOwner(userId);
      allow delete: if false;
    }

    // Catches — read public, write vlastné
    match /catches/{catchId} {
      allow read: if isAuth();
      allow create: if isAuth() && request.resource.data.userId == request.auth.uid;
      allow update, delete: if isOwner(resource.data.userId);
    }

    // Spots — read public, write len admin SDK
    match /spots/{spotId} {
      allow read: if true;
      allow write: if false;
    }
  }
}
```

**DÔLEŽITÉ pre developera:** Vytvoriť Firebase projekt na console.firebase.google.com, stiahnuť google-services.json a GoogleService-Info.plist a nahradiť placeholders pred EAS buildom.
  </action>
  <verify>
    <automated>cd "C:/Users/Počítač/Documents/catch-app" && node -e "const a = require('./app.json'); const plugins = a.expo.plugins; const hasFirebase = plugins.includes('@react-native-firebase/app'); const hasBuildProps = plugins.some(p => Array.isArray(p) && p[0] === 'expo-build-properties'); console.log('Firebase plugin:', hasFirebase, '| build-properties:', hasBuildProps, '| bundleId:', a.expo.android.package)"</automated>
  </verify>
  <done>app.json má Firebase plugin, expo-build-properties s iOS useFrameworks:static, bundle ID sk.catchapp.app. eas.json má 3 profily. src/lib/firebase.ts existuje. firestore.rules má skeleton pravidlá.</done>
</task>

</tasks>

<verification>
```bash
cd "C:/Users/Počítač/Documents/catch-app"
# Overiť štruktúru projektu
node -e "const p = require('./package.json'); console.log('Dependencies count:', Object.keys(p.dependencies || {}).length)"
# Overiť app.json
node -e "const a = require('./app.json'); console.log('Scheme:', a.expo.scheme, '| Package:', a.expo.android.package)"
# Overiť eas.json
node -e "const e = require('./eas.json'); console.log('EAS profiles:', Object.keys(e.build).join(', '))"
```
</verification>

<success_criteria>
- Expo SDK 53 projekt s platným package.json a všetkými závislosti
- app.json nakonfigurovaný s Firebase pluginmi, iOS useFrameworks:static, bundle ID sk.catchapp.app
- eas.json s development/preview/production profilmi
- src/lib/firebase.ts s centralizovanými importmi (bez initializeApp)
- firestore.rules so skeleton pravidlami pre users/catches/spots
- Placeholder Firebase config súbory s jasnými inštrukciami pre developera
</success_criteria>

<output>
Po dokončení vytvoriť `.planning/phases/01-foundation-auth/01-01-SUMMARY.md` s:
- Zoznam nainštalovaných závislostí a ich verzií
- Skutočná verzia @react-native-firebase/app (overiť po npm install)
- Akékoľvek odchýlky od plánu
- Stav pre ďalšie plány
</output>
