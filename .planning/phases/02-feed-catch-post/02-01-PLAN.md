---
phase: 02-feed-catch-post
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/__tests__/unit/imageUpload.test.ts
  - src/__tests__/unit/catches.test.ts
  - src/__tests__/unit/fishSpecies.test.ts
  - src/__tests__/unit/weather.test.ts
  - src/__tests__/unit/profile.test.ts
autonomous: true
requirements: [PHOTO-01, PHOTO-02, PHOTO-03, PHOTO-04, DIARY-01, DIARY-02, DIARY-06, DIARY-07, PROF-01, PROF-02, PROF-04, PROF-05]

must_haves:
  truths:
    - "Všetky nové knižnice sú nainštalované bez konfliktov"
    - "5 test súborov existuje a testy prechádzajú (passWithNoTests)"
    - "npm test beží bez erroru v < 15 sekúnd"
  artifacts:
    - path: "src/__tests__/unit/imageUpload.test.ts"
      provides: "Test scaffold pre PHOTO-01 až PHOTO-04"
    - path: "src/__tests__/unit/catches.test.ts"
      provides: "Test scaffold pre DIARY-01 až DIARY-06"
    - path: "src/__tests__/unit/fishSpecies.test.ts"
      provides: "Test scaffold pre DIARY-07"
    - path: "src/__tests__/unit/weather.test.ts"
      provides: "Test scaffold pre DIARY-02"
    - path: "src/__tests__/unit/profile.test.ts"
      provides: "Test scaffold pre PROF-01 až PROF-05"
  key_links:
    - from: "package.json"
      to: "src/__tests__/unit/*.test.ts"
      via: "jest-expo preset"
      pattern: "jest-expo"
---

<objective>
Nainštalovať všetky nové závislosti pre Phase 2 a vytvoriť Wave 0 test scaffolding — prázdne test súbory s describe/it blokmi pre každý requirement. Tieto testy budú v stave "passing (no tests)" a postupne sa naplnia počas implementácie.

Purpose: Nyquist compliance — každý task v ďalších plánoch má existujúci test súbor na spustenie.
Output: 5 test súborov + nainštalované knižnice (expo-image, expo-image-manipulator, expo-image-picker, expo-location, @shopify/flash-list, suncalc).
</objective>

<execution_context>
@C:/Users/Počítač/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Počítač/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-feed-catch-post/02-CONTEXT.md
@.planning/phases/02-feed-catch-post/02-RESEARCH.md
@.planning/phases/02-feed-catch-post/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inštalácia Phase 2 závislostí</name>
  <files>package.json</files>
  <action>
Spusti inštaláciu nových knižníc:

```bash
cd C:/Users/Počítač/Documents/catch-app
npx expo install expo-image expo-image-manipulator expo-image-picker expo-location @shopify/flash-list
npm install suncalc
npm install --save-dev @types/suncalc
```

Po inštalácii over `package.json` — musia byť prítomné:
- `expo-image` (~55.0.x)
- `expo-image-manipulator` (~55.0.x)
- `expo-image-picker` (~55.0.x)
- `expo-location` (~55.0.x)
- `@shopify/flash-list` (^2.0.0)
- `suncalc` (^1.9.0)

DÔLEŽITÉ: `@shopify/flash-list` musí byť verzia 2.x — nie 1.x. FlashList v2 je New Architecture only (Expo SDK 55 = New Architecture). V2 nemá `estimatedItemSize` — nepoužívaj ho nikde.

Expo-location vyžaduje tiež config plugin v app.json. Pridaj do `plugins` array v `app.json`:
```json
["expo-location", { "locationAlwaysAndWhenInUsePermission": "Catch potrebuje prístup k polohe pre automatické doplnenie počasia." }]
```

Expo-image-picker config plugin (ak nie je):
```json
["expo-image-picker", { "photosPermission": "Catch potrebuje prístup k fotogalérii pre nahratie fotiek úlovkov." }]
```
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const deps={...p.dependencies,...p.devDependencies}; ['expo-image','expo-image-manipulator','expo-image-picker','expo-location','@shopify/flash-list','suncalc'].forEach(d=>{ if(!deps[d]) throw new Error('Missing: '+d); console.log('OK:',d,deps[d]); })"</automated>
  </verify>
  <done>Všetkých 6 knižníc je v package.json bez npm install erroru. @shopify/flash-list je 2.x.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 — Test scaffolding (5 súborov)</name>
  <files>
    src/__tests__/unit/imageUpload.test.ts,
    src/__tests__/unit/catches.test.ts,
    src/__tests__/unit/fishSpecies.test.ts,
    src/__tests__/unit/weather.test.ts,
    src/__tests__/unit/profile.test.ts
  </files>
  <action>
Vytvor adresár `src/__tests__/unit/` ak neexistuje. Potom vytvor 5 test súborov:

**`src/__tests__/unit/imageUpload.test.ts`**
```typescript
// Wave 0 scaffold — testy sa naplnia v 02-02 (image upload pipeline)
// Pokrýva: PHOTO-01, PHOTO-02, PHOTO-03, PHOTO-04

describe('imageUpload — photo pipeline', () => {
  describe('PHOTO-01: ImagePicker multi-select', () => {
    it.todo('should return max 5 assets from launchImageLibraryAsync');
    it.todo('should not return more than 5 assets when selectionLimit=5');
  });

  describe('PHOTO-02: Compression < 500KB', () => {
    it.todo('compressed image should be under 500KB');
    it.todo('should resize to max 1200px width');
  });

  describe('PHOTO-03: Upload progress callback', () => {
    it.todo('onProgress callback should be called during upload');
    it.todo('onProgress value should be between 0 and 1');
  });

  describe('PHOTO-04: Blurhash generation', () => {
    it.todo('generateBlurhashAsync should return a non-empty string');
    it.todo('blurhash should be generated from local URI before upload');
  });
});
```

**`src/__tests__/unit/catches.test.ts`**
```typescript
// Wave 0 scaffold — testy sa naplnia v 02-02, 02-04, 02-05
// Pokrýva: DIARY-01, DIARY-02, DIARY-03, DIARY-04, DIARY-05, DIARY-06

describe('catches — Firestore CRUD a schema', () => {
  describe('DIARY-01: Catch schema validation', () => {
    it.todo('catch document should have all required fields');
    it.todo('weightG should be in grams (number)');
    it.todo('photos array should contain downloadURL and blurhash');
  });

  describe('DIARY-03: Filter queries', () => {
    it.todo('filter by species should add correct where clause');
    it.todo('filter by date range should add correct where clauses');
  });

  describe('DIARY-04: Catch detail — weather snapshot', () => {
    it.todo('catch detail should contain weather snapshot from creation time');
  });

  describe('DIARY-05: Edit and delete', () => {
    it.todo('delete catch should call Firestore delete on correct path');
    it.todo('update catch should call Firestore update with changed fields');
  });

  describe('DIARY-06: Fire-and-forget offline write', () => {
    it.todo('createCatch should call docRef.set without await');
    it.todo('createCatch should not throw when offline');
  });
});
```

**`src/__tests__/unit/fishSpecies.test.ts`**
```typescript
// Wave 0 scaffold — test sa naplní v 02-03 (fish species data)
// Pokrýva: DIARY-07

describe('fishSpecies — SK/CZ species list', () => {
  describe('DIARY-07: Species list completeness', () => {
    it.todo('should contain 100 or more species');
    it.todo('each species should have id, name (SK), and latinName');
    it.todo('should include Kapor (common carp)');
    it.todo('should include Šťuka (northern pike)');
    it.todo('search filter should return matching species case-insensitively');
  });
});
```

**`src/__tests__/unit/weather.test.ts`**
```typescript
// Wave 0 scaffold — test sa naplní v 02-03 (weather service)
// Pokrýva: DIARY-02

describe('weather — Open-Meteo + suncalc', () => {
  describe('DIARY-02: Weather auto-fetch', () => {
    it.todo('fetchWeatherForCatch should return temperature, pressure, windSpeed, precipitation');
    it.todo('fetchWeatherForCatch should return moonPhase string');
    it.todo('fetchWeatherForCatch should return moonIllumination 0-100');
    it.todo('should use wind_speed_10m param (not windspeed_10m)');
    it.todo('getMoonPhaseName should return Slovak names');
    it.todo('getMoonPhaseName(0) should return Novoluní');
    it.todo('getMoonPhaseName(0.5) should return Spln');
  });
});
```

**`src/__tests__/unit/profile.test.ts`**
```typescript
// Wave 0 scaffold — testy sa naplnia v 02-08 (profile screen)
// Pokrýva: PROF-01, PROF-02, PROF-03, PROF-04, PROF-05

describe('profile — user document a štatistiky', () => {
  describe('PROF-01: User document schema', () => {
    it.todo('user document should have displayName, username, bio fields');
    it.todo('bio should be max 300 characters');
    it.todo('user document should have avatarURL and avatarBlurhash');
  });

  describe('PROF-02: Stats aggregation', () => {
    it.todo('getUserStats should return catchCount correctly');
    it.todo('getUserStats should return totalWeightG as sum');
    it.todo('getUserStats should return biggestCatchG as max');
  });

  describe('PROF-03: Last 12 catches query', () => {
    it.todo('catches query should have limit(12)');
    it.todo('catches query should orderBy createdAt descending');
  });

  describe('PROF-04: isOwnProfile logic', () => {
    it.todo('isOwnProfile should be true when currentUserId === profileId');
    it.todo('isOwnProfile should be false when currentUserId !== profileId');
  });

  describe('PROF-05: Karma and badges', () => {
    it.todo('user document should have karma field (number)');
    it.todo('user document should have badges field (array)');
  });
});
```
  </action>
  <verify>
    <automated>cd C:/Users/Počítač/Documents/catch-app && npm test -- --testPathPattern='__tests__/unit' --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>5 test súborov existuje v src/__tests__/unit/. `npm test -- --passWithNoTests` prebehne bez erroru. Všetky testy sú v stave `todo` (nie failing).</done>
</task>

</tasks>

<verification>
1. `node -e "require('./package.json').dependencies['@shopify/flash-list']"` — vypíše verziu 2.x
2. `ls src/__tests__/unit/` — zobrazí 5 súborov
3. `npm test -- --passWithNoTests` — žiadny error, testy sú todo
</verification>

<success_criteria>
- 6 nových knižníc v package.json bez konfliktov
- 5 test scaffolding súborov v src/__tests__/unit/
- npm test prechádza (passWithNoTests) za menej ako 15 sekúnd
- @shopify/flash-list je verzia ^2.0.0 (nie 1.x)
</success_criteria>

<output>
After completion, create `.planning/phases/02-feed-catch-post/02-01-SUMMARY.md`
</output>
