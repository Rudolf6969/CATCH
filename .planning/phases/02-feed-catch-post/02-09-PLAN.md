---
phase: 02-feed-catch-post
plan: 09
type: execute
wave: 7
depends_on: [02-05, 02-06, 02-07, 02-08]
files_modified: []
autonomous: false
requirements: [PHOTO-01, PHOTO-02, PHOTO-03, PHOTO-04, DIARY-01, DIARY-02, DIARY-03, DIARY-04, DIARY-05, DIARY-06, DIARY-07, PROF-01, PROF-02, PROF-03, PROF-04, PROF-05]

must_haves:
  truths:
    - "Catch wizard prejde od FAB do submit za < 90 sekúnd"
    - "Blurhash placeholder je viditeľný pred načítaním plnej fotky"
    - "Pridanie úlovku offline sa objaví okamžite v denníku"
    - "Profil zobrazuje správne štatistiky a grid úlovkov"
    - "Všetky unit testy zelené"
  artifacts:
    - path: "app/catch/new/step-1.tsx"
      provides: "Photo picker working"
    - path: "app/(tabs)/feed.tsx"
      provides: "Feed s reálnymi dátami"
    - path: "app/profile/[userId].tsx"
      provides: "Profil screen functional"
  key_links:
    - from: "app/(tabs)/pridat.tsx"
      to: "app/catch/new/step-1.tsx"
      via: "Redirect"
      pattern: "Redirect"
---

<objective>
Verifikácia Phase 2 — human QA checkpoint pre vizuálne a funkcionálne overenie celého catch flow.

Purpose: Overiť že celý loop (foto → wizard → feed → denník → profil) funguje na reálnom zariadení.
Output: Schválenie Phase 2 pre pokračovanie na Phase 3.
</objective>

<execution_context>
@C:/Users/Počítač/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Počítač/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-feed-catch-post/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Celková test suite + TypeScript check</name>
  <files></files>
  <action>
Spusti celú test suite a TypeScript kompiláciu:

```bash
cd C:/Users/Počítač/Documents/catch-app
npx tsc --noEmit
npm test -- --passWithNoTests
```

Ak testy zlyhajú, oprav chyby pred human verify.

Skontroluj aj tieto kľúčové anti-patterny:

1. FlashList bez estimatedItemSize:
   - `grep -r "estimatedItemSize" app/ src/` — NESMIE nájsť nič

2. Správny wind_speed_10m parameter (nie windspeed_10m):
   - `grep -r "windspeed_10m" src/` — NESMIE nájsť nič

3. Fire-and-forget write (bez await na set):
   - `grep -n "await.*docRef.set\|await.*\.set(catchData)" src/hooks/useCatches.ts` — NESMIE nájsť

4. Blurhash z local URI (nie z downloadURL):
   - `grep -n "generateBlurhashAsync" src/services/imageUpload.ts` — MUSÍ byť prítomné

5. FlashList v2 import:
   - `grep -r "from '@shopify/flash-list'" app/` — MUSÍ byť v feed.tsx a dennik.tsx
  </action>
  <verify>
    <automated>cd C:/Users/Počítač/Documents/catch-app && npm test -- --passWithNoTests 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>Všetky testy zelené. TypeScript kompiluje bez chýb. Žiadne anti-patterny v kóde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Celá Phase 2: Catch Diary + Feed + Profil

    Implementované funkcie:
    - 3-krokový catch wizard (FAB → foto → ryba/počasie → lokalita/submit)
    - Image upload pipeline (kompressia <500KB, blurhash, Firebase Storage)
    - Feed screen (FlashList v2, reálne Firestore dáta, pull-to-refresh)
    - Denník screen (vlastné úlovky, filter, FlashList v2)
    - Catch detail screen (všetky parametre, počasie, delete)
    - Profil screen (Twitter-style, stats, 3-stĺpcový grid, karma/badges)
  </what-built>
  <how-to-verify>
    Otvor EAS dev build na reálnom zariadení (nie Expo Go) a overi:

    **1. Catch Wizard (DIARY-01 — <90 sekúnd)**
    - Stlač + FAB → otvorí sa krok 1 (foto picker)
    - Vyber 2 fotky z galérie (multi-select) alebo odfot kamerou
    - Potvrď "Ďalej →" → krok 2 (ryba + počasie)
    - Vidíš WeatherBadge s teplota/tlak/vietor/mesiac?
    - Vyber rybu (hľadaj "Kapor") → zadaj váhu a dĺžku
    - "Ďalej →" → krok 3 (lokalita + caption + submit)
    - Zadaj poznámku → "Uložiť úlovok"
    - Meranie celkového času: musí byť < 90 sekúnd

    **2. Feed (PHOTO-04 — blurhash)**
    - Prejdi na Feed tab
    - Vidíš nový úlovok v zozname?
    - Odfot screen pred načítaním plnej fotky — vidíš blurhash placeholder? (môže byť rýchle)
    - Pull-to-refresh funguje?
    - Tap na avatar/meno → presunie na profil?

    **3. Offline test (DIARY-06)**
    - Zapni Airplane Mode
    - Pridaj úlovok cez wizard
    - Úlovok sa objaví okamžite v Denníku (optimistický UI)?
    - Vypni Airplane Mode → úlovok sa synchronizuje?

    **4. Denník (DIARY-03 — filter)**
    - Prejdi na Denník tab
    - Stlač "Filtre" → vyber metódu
    - Zoznam sa filtruje?

    **5. Profil (PROF-02 — stats)**
    - Tap na avatar v headeri Feedu → otvorí profil
    - Vidíš Stats riadok: Úlovky · kg celkom · kg rekord?
    - Grid 3 stĺpce s úlovkami?
    - Tap na grid fotku → otvorí catch detail s počasím?
  </how-to-verify>
  <resume-signal>
    Napíš "schválené" ak všetko funguje, alebo opíš konkrétne problémy pre opravu.
  </resume-signal>
</task>

</tasks>

<verification>
1. `npm test -- --passWithNoTests` — zelené pred human verify
2. `npx tsc --noEmit` — žiadne TS chyby
3. Human verify: catch wizard < 90s, blurhash viditeľný, offline funguje, profil stats správne
</verification>

<success_criteria>
Phase 2 je kompletná keď:
1. Catch wizard prejde celý flow za < 90 sekúnd
2. Fotka sa zobrazí s blurhash placeholder pred plným načítaním
3. Pridanie úlovku offline je okamžite viditeľné v Denníku
4. Profil zobrazuje stats (catchCount, totalWeight, biggest) a grid 12 úlovkov
5. Všetky unit testy zelené (16/16 requirements pokrytých)
</success_criteria>

<output>
After completion, create `.planning/phases/02-feed-catch-post/02-09-SUMMARY.md`
</output>
